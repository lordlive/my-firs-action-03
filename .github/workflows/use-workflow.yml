name: use-workflow

on:
  push:
    branches:
      - rc-*
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger-manual:
    name: Trigger manual action
    uses: lordlive/standart-workflows/.github/workflows/trigger.yml@v1.0.0
    with:
      workflow-ref: v1.1.0
      workflow-path: .github/workflows/manual.yml
      build-version: v1.1.0
